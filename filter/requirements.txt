# ── 核心 NLP 依赖 ─────────────────────────────────────────────
# fastText 语种识别（wheel 版，避免编译 C++ 依赖）
fasttext-wheel==0.9.2

# FlashText 高性能关键词匹配（时间复杂度 O(n)，远优于正则）
flashtext==2.7

# 语音哈希 / 字符串距离（Double Metaphone, Levenshtein 等）
jellyfish==1.0.3

# 拼音转写（中文 ASR 容错）
pypinyin==0.51.0

# ── 数据模型与类型校验 ────────────────────────────────────────
pydantic==2.7.1

# ── Unicode 处理 ──────────────────────────────────────────────
# Python 标准库 unicodedata 已覆盖 NFKC，无需额外依赖

# ── 工具与测试 ────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0

# ── 可选：SimHash/MinHash（阶段一 Bot 锚点计算，可按需启用）──
simhash==2.1.2
datasketch==1.6.4
