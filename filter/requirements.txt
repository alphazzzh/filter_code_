# ── Web 服务框架 ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0

# ── LLM 客户端 ────────────────────────────────────────────────
openai>=1.30.0

# ── 数据模型与类型校验 ────────────────────────────────────────
pydantic>=2.7.1

# ── 核心 NLP 依赖 ─────────────────────────────────────────────
# fastText 语种识别（wheel 版，避免编译 C++ 依赖）
fasttext-wheel==0.9.2

# 语音哈希 / 字符串距离（Double Metaphone, Soundex 等）
jellyfish>=1.0.3

# 拼音转写（中文 ASR 容错）
pypinyin>=0.51.0

# ── 向量计算 ──────────────────────────────────────────────────
numpy>=1.26.0
scikit-learn>=1.4.0

# ── 可选：中文 NLP 后端（按需安装其一）───────────────────────
# LTP 依存句法 + NER（推荐，速度更快）
# ltp>=4.2.0
# torch>=2.0.0          # ltp 依赖 PyTorch

# HanLP 多任务 NLP（备选）
# hanlp>=2.1.0

# ── 可选：向量语义匹配（IntentRadar BGE-M3 模式）─────────────
# FlagEmbedding>=1.2.0
# torch>=2.0.0

# ── Unicode 处理 ──────────────────────────────────────────────
# Python 标准库 unicodedata 已覆盖 NFKC，无需额外依赖

# ── 工具与测试 ────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0

# ── 可选：SimHash/MinHash（Bot 锚点计算，可按需启用）──────────
# simhash==2.1.2
# datasketch==1.6.4
