# ── Web 服务框架 ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0

# ── 大模型与工作流编排 (新增 LangGraph) ────────────────────────
openai>=1.30.0
langgraph>=0.0.65
langchain-core>=0.2.0

# ── 数据模型与类型校验 ────────────────────────────────────────
pydantic>=2.7.1

# ── 核心 NLP 依赖 ─────────────────────────────────────────────
# fastText 语种识别（wheel 版，避免编译 C++ 依赖）
fasttext-wheel==0.9.2

# 语音哈希 / 字符串距离（Double Metaphone, Soundex 等）
jellyfish>=1.0.3

# 拼音转写（中文 ASR 容错）
pypinyin>=0.51.0

# ── 向量计算 ──────────────────────────────────────────────────
numpy>=1.26.0
scikit-learn>=1.4.0

# ── 深度学习与核心 NLP 后端 (全量跑通必须解除注释) ───────────────
torch>=2.0.0
ltp>=4.2.0              # LTP 依存句法分析
FlagEmbedding>=1.2.0    # BGE-M3 向量语义匹配

# ── 可选组件 (目前暂不需要，保持注释) ──────────────────────────
# hanlp>=2.1.0
simhash==2.1.2
# datasketch==1.6.4